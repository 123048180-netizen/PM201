// utils.js

// Función que recibe dos números y retorna la resta
function restar(a, b) {
    return a - b;
}

// Exportamos la función para poder utilizarla en otros archivos
module.exports = {
    restar
};

